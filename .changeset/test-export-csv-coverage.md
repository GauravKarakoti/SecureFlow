---
"secureflow": patch
---

Cover `src/lib/utils/exportCsv.ts`, the audit log's client-side download helper, which shipped at 0% — including the BOM, the shared formula-injection defence, and the object-URL release on a failed click.
