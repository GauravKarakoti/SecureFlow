{
  "name": "secureflow-cli",
  "version": "1.1.0",
  "description": "Pre-commit secret logging detector for secure development workflows",
  "main": "./dist/index.js",
  "bin": {
    "secureflow": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^6",
    "vitest": "^4.1.10"
  }
}
